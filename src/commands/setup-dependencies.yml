description: >
  This command setup all the dependencies required for the orb.
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: Setup Dependencies
      command: <<include(scripts/setup-dependencies.sh)>>
