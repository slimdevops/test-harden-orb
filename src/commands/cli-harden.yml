description: >
  This command create a Hardened Image.
# What will this command do?
# Descriptions should be short, simple, and clear.

steps:
  - run:
      name: HARDEN IMAGE
      command: <<include(scripts/cli-harden.sh)>>
